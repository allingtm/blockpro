import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add app smoke tests once auth mocking is set up
    expect(true, isTrue);
  });
}
